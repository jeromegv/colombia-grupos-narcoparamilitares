extends layout

block content
    h1= title
    div(id='map')
    for group in groups
        div(class="checkbox")
            label
                input(type="checkbox" name="groups[]" value=group.name onclick="updateMap()" checked=true) 
                |#{group.name}